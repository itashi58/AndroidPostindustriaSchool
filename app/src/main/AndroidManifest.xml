<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.androidpostindustriaschool">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true">

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key"
            />

        <activity
            android:name=".ui.maps_activity.MapsActivity"
            android:label="@string/title_activity_maps"
            android:theme="@style/Theme.AndroidPostindustriaSchoolActionBar" />
        <activity
            android:name=".ui.history_activity.HistoryActivity"
            android:label="@string/label_history_actionBar"
            android:theme="@style/Theme.AndroidPostindustriaSchoolActionBar" />
        <activity
            android:name=".ui.favorites_activity.FavoritesActivity"
            android:label="@string/label_favorites_actionBar"
            android:theme="@style/Theme.AndroidPostindustriaSchoolActionBar" />
        <activity
            android:name=".ui.login_activity.LoginActivity"
            android:label="@string/title_activity_login"
            android:theme="@style/Theme.AndroidPostindustriaSchool" />
        <activity
            android:name=".ui.view_activity.ViewActivity"
            android:theme="@style/Theme.AndroidPostindustriaSchool"
            tools:ignore="AppLinkUrlError">

            <!-- <intent-filter> -->
            <!-- <action android:name="android.intent.action.VIEW" /> -->


            <!-- <category android:name="android.intent.category.DEFAULT" /> -->


            <!-- <data android:scheme="https" /> -->
            <!-- <data android:scheme="http" /> -->
            <!-- </intent-filter> -->
        </activity>
        <activity
            android:name=".ui.splash_screen_activity.SplashScreenActivity"
            android:theme="@style/Theme.AndroidPostindustriaSchool">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.main_activity.MainActivity"
            android:theme="@style/Theme.AndroidPostindustriaSchool" />
    </application>

</manifest>